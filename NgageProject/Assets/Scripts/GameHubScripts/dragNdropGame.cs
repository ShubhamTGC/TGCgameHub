using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class dragNdropGame : MonoBehaviour
{
    // Start is called before the first frame update
    public List<string> Ale, Lager, Mead, Stout, Bourbon, Rye, Scotch, Swm, Red, Rose, Sparkling, white;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
